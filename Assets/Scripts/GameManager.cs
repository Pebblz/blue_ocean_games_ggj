using UnityEngine;

public class GameManager : MonoBehaviour, GameDataPersistence
{
    public void LoadData(GameData data)
    { 
        //will add stuff to this later
    }

    public void SaveData(ref GameData data)
    {
        //will add stuff to this later
    }
}